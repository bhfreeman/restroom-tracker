<div class="columns column is-full">
    <div class="column">
      <div>
          <h1 class="is-size-3 mb-3 has-text-weight-bold">Search Results</h1>
          </div>
    <div class="columns column is-full is-flex-wrap-wrap">
        
            {{> individual-bathroom }}
            <div class="columns column is-flex-wrap-wrap is-offset-1 is-multiline m-2 is-5 box ml-4">
                {{#each bathroom.reviews}}
                {{>review}}
                {{!-- <div class="column"> --}}
                {{!-- {{#each comments}}
                {{>comment}}
                {{/each}}
                </div> --}}
                {{/each}}
            </div>
        
    <div class="column is-one-quarter box m-2">
        <div class="form">
            <div class="field block"><div>
          <h1 class="is-size-5 mb-3 has-text-weight-bold">Add a Review</h1>
          </div>
                <label class="label">Title</label>
                <div class="control">
                    <input class="input" type="text" placeholder="Enter Review Title" id="title">
                </div>

            </div>
            <div class="field block">

                <label class="label">Review Text</label>
                <div class="control">
                    <input class="input" type="textarea" placeholder="Enter Your Review Here" id="review-text">
                </div>
            </div>
            <div class="field block">
                <label class="label">Rating</label>
                <div class="control">
                    <select name="rating" id="rating">
                        <option value="1">1</option>
                        <option value="2">2</option>
                        <option value="3">3</option>
                        <option value="4">4</option>
                        <option value="5">5</option>
                    </select>
                </div>
            </div>
            <div class="field block m-4">
                <button class="button is-primary is-focused mr-4" id="submit-review">
                    Add Review
                </button>
            </div>
        </div>
    </div></div>
    </div>
    </div>
</div>

<script src="/js/review.js"></script>